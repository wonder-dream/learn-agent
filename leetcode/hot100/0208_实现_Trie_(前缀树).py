# 208. 实现 Trie (前缀树) (Implement Trie)
# 难度：中等
# 分类：图论
# https://leetcode.cn/problems/implement-trie-prefix-tree/

"""
    Trie（发音类似 "try"）或者说前缀树是一种树形数据结构，用于高效地存储和检索字符串数据集中的键。
    实现 Trie 类：
    - Trie() 初始化前缀树对象。
    - void insert(String word) 向前缀树中插入字符串 word。
    - boolean search(String word) 如果字符串 word 在前缀树中，返回 true；否则返回 false。
    - boolean startsWith(String prefix) 如果之前已经插入的字符串 word 的前缀之一为 prefix，返回 true；否则返回 false。
    """
    # 此题非 Solution 类，在下方 __main__ 区域自己写 Trie 类
    pass

class Solution:
    # 请根据题意补充方法签名



if __name__ == "__main__":
    s = Solution()
    # 在此写本地测试用例
    pass
