# Day 05 — pytest + 项目骨架初始化

**日期**: 2026-06-28
**里程碑**: M1 基础筑基
**主题**: 测试框架 + 项目结构搭建

---

## 今日目标

- [x] 掌握 pytest 核心用法：fixture, parametrize, mock, raises
- [x] 理解测试金字塔
- [x] 给 `projects/code-review-agent/` 写第一个可运行的测试
- [x] 笔记输出到 `notes/engineering/pytest-basics.md`



## 前置知识（今日之前应已掌握）
<!-- 告诉 Claude 你的起点，避免重复讲解或假设过高 -->

- 

## 核心概念清单
<!-- Claude 会按这个清单逐一掰开揉碎讲解 -->

1. **pytest fixture**: 把测试的准备工作和测试逻辑分离，让数据复用
2. **pytest parametrize**: 同一段测试逻辑用不同输入跑多次
2. **pytest.raises*: **断言"某段代码应该抛异常"，抛了就是通过，没抛就是失败
2. **mock: **用一个假对象替换真实依赖，让你能控制外部行为的返回值

## 具体任务

### 1. pytest 核心 (1h)

```bash
pip install pytest pytest-asyncio pytest-mock
```

```python
import pytest
from src.schemas import ReviewRequest

# 基础断言
def test_valid_review_request():
    req = ReviewRequest(
        file_path="test.py",
        file_content="print('hello')"
    )
    assert req.language == "python"
    assert req.review_level == "medium"  # 默认值

# 异常断言
def test_invalid_line_number():
    from src.schemas import ReviewComment
    from pydantic import ValidationError
    with pytest.raises(ValidationError):
        ReviewComment(
            file_path="x.py",
            line_number=0,      # 无效！
            severity="error",
            category="bug",
            message="test"
        )

# fixture：复用测试数据
@pytest.fixture
def sample_request():
    return ReviewRequest(
        file_path="test.py",
        file_content="def foo():\n    pass\n",
    )

def test_with_fixture(sample_request):
    assert sample_request.file_path == "test.py"

# parametrize：一组输入跑同一测试
@pytest.mark.parametrize("language", ["python", "java", "go"])
def test_languages(language):
    req = ReviewRequest(
        file_path="x." + language,
        file_content="",
        language=language
    )
    assert req.language == language
```

### 2. mock 入门 (0.5h)

```python
# mock 的含义：测试时不真正调用外部服务
def test_review_with_mock_llm(mocker):
    # mock LLM 客户端，让它返回假数据
    mock_client = mocker.patch("src.llm_client.LLMClient.chat")
    mock_client.return_value = "No issues found."

    # 接下来测试审查逻辑，不需要真的调 LLM
    result = review_code("some code")
    assert result is not None
```

### 3. 项目测试实战 (1.5h)

```bash
cd projects/code-review-agent
pip install -e ".[dev]"
```

在 `tests/test_schemas.py` 里写：
- [x] 正常 ReviewRequest 解析
- [x] 非法 line_number 抛异常
- [x] 空 message 抛异常
- [x] 未知 severity 抛异常
- [x] model_dump() 输出格式验证

然后跑 `pytest -v`，看到全绿。

### 4. 项目结构确认 (0.5h)

确保目录结构对齐：
```
projects/code-review-agent/
├── src/
│   ├── __init__.py
│   ├── schemas.py       # ← Day 03 写的
│   ├── llm_client.py    # ← 占位
│   ├── config.py        # ← 占位
│   └── simple_agent.py  # ← 占位
├── tests/
│   ├── __init__.py
│   └── test_schemas.py  # ← 今天写的
├── pyproject.toml
└── README.md
```

### 5. 输出 (0.5h)
- 笔记整理到 `notes/engineering/pytest-basics.md`

## LeetCode 每日

- [x] 环形链表 (Easy)
- [x] 回文链表 (Easy)

---

## 我的疑问（课前填写）
<!-- 让 Claude 针对性解答，而不是泛泛而谈 -->

1. 
2. 

---

## 完成情况

<!-- 当天结束时填写 -->

- 实际用时: 2.5h
- 完成度: 100%
- 卡住的地方: 写在笔记中了
- 代码提交: 提交了

## 复盘笔记（课后填写）
<!-- 下次 Claude 会读取这个，调整后续讲解深度 -->

- 理解最透彻的：原理部分都还比较好理解
- 仍然模糊的：day 01 - 05 各种方法的具体用法，在实战中的具体使用不熟悉
- 想深入的方向：pytest 只有这些内容吗？没有其他内容了吗？感觉比起 day04 内容少了好多

## 明天计划

FastAPI 基础：路由 + 请求/响应模型

---
*打卡时间: 23:59 前 commit*
