# Day 06 — FastAPI 基础：路由 + 请求/响应模型

**日期**: 2026-06-29
**里程碑**: M1 基础筑基
**主题**: FastAPI 入门 + 对接项目 Schema

---

## 今日目标

- [x] 跑通第一个 FastAPI 应用
- [x] 掌握路由定义（GET/POST）+ 路径参数/查询参数/请求体
- [x] 结合 pydantic 做请求校验 + 响应序列化
- [x] 笔记输出到 `notes/engineering/fastapi-basics.md`


## 前置知识（今日之前应已掌握）
<!-- 告诉 Claude 你的起点，避免重复讲解或假设过高 -->

- 

## 核心概念清单
<!-- Claude 会按这个清单逐一掰开揉碎讲解 -->

1. **概念 A**：一句话定义
2. **概念 B**：一句话定义


## 具体任务

### 1. Hello World (0.5h)

```bash
pip install fastapi uvicorn
```

```python
# main.py
from fastapi import FastAPI

app = FastAPI(title="Code Review Agent")

@app.get("/health")
async def health():
    return {"status": "ok"}

# uvicorn main:app --reload
# 然后打开 http://localhost:8000/docs 看 Swagger UI
```

### 2. 路由 + 参数 (1.5h)

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel

# 路径参数
@app.get("/reviews/{review_id}")
async def get_review(review_id: str):
    return {"review_id": review_id}

# 查询参数
@app.get("/reviews")
async def list_reviews(status: str = "all", limit: int = 10):
    return {"status": status, "limit": limit}

# 请求体（POST）
class CreateReviewRequest(BaseModel):
    file_path: str
    file_content: str
    language: str = "python"

@app.post("/reviews")
async def create_review(req: CreateReviewRequest):
    # req 是 pydantic 自动校验后的 CreateReviewRequest 实例
    return {"id": "fake-id", "file": req.file_path}

# 异常响应
@app.get("/reviews/{review_id}")
async def get_review(review_id: int):
    if review_id < 0:
        raise HTTPException(status_code=400, detail="Invalid ID")
    return {"id": review_id}
```

### 3. 对接项目 Schema (1h)

把 Day 03 写的 `ReviewRequest` / `ReviewResponse` 接入 FastAPI 接口：

```python
from src.schemas import ReviewRequest, ReviewResponse

@app.post("/review", response_model=ReviewResponse)
async def review_code(req: ReviewRequest) -> ReviewResponse:
    # 先返回假数据，后续接真正的 Agent
    return ReviewResponse(
        request_id="fake-id",
        comments=[],
        summary="Mock review",
        total_issues=0,
    )
```

### 4. 输出 (0.5h)
- 笔记整理到 `notes/engineering/fastapi-basics.md`
- 重点记录：路径参数 vs 查询参数、response_model 的作用、自动 docs

## LeetCode 每日

- [ ] 删除排序链表中的重复元素 (Easy)
- [ ] 合并两个有序链表 (Easy)

---

## 我的疑问（课前填写）
<!-- 让 Claude 针对性解答，而不是泛泛而谈 -->

1. fastapi 只有这么点内容吗？只有这些路由和参数吗？
2. 给我详细讲解一下我想做 agent 开发需要使用到关于 fastapi 的所有内容。

---

## 完成情况

<!-- 当天结束时填写 -->

- 实际用时:2h
- 完成度:90%
- 卡住的地方:没什么特别卡住的
- 代码提交: <!-- 链接或 commit -->

## 明天计划

---
*打卡时间: 23:59 前 commit*
