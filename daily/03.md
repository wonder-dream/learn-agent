# Day 03 — 类型注解 + pydantic

**日期**: 2026-06-26
**里程碑**: M1 基础筑基
**主题**: Python 类型系统 + 数据校验

---

## 今日目标

- [x] 理解 Python type hints 的常用类型
- [x] 掌握 pydantic v2 的 BaseModel、Field
- [x] 给 code-review-agent 定义核心数据模型
- [x] 笔记输出到 `notes/engineering/python-types.md`

## 具体任务

### 1. Type Hints (1h)

```python
from typing import Optional, Union, Literal

# 基础类型
def greet(name: str, age: int) -> str:
    return f"{name} is {age}"

# Optional = 可以是 None
def find_user(id: int) -> Optional[dict]:
    ...

# Union = 多种类型之一
def process(data: Union[str, bytes]) -> str:
    ...

# Literal = 限定值的范围
ReviewLevel = Literal["low", "medium", "high"]
Severity = Literal["info", "warning", "error", "critical"]
```

### 2. pydantic v2 入门 (1.5h)

```bash
pip install pydantic
```

```python
from pydantic import BaseModel, Field
from typing import Optional
from enum import Enum

class Severity(str, Enum):
    INFO = "info"
    WARNING = "warning"
    ERROR = "error"
    CRITICAL = "critical"

class ReviewComment(BaseModel):
    file_path: str
    line_number: int = Field(ge=1, description="行号从 1 开始")
    severity: Severity
    category: str = Field(description="style/naming/logic/security")
    message: str = Field(min_length=1, max_length=200)
    suggestion: Optional[str] = None

class ReviewRequest(BaseModel):
    file_path: str
    file_content: str
    language: str = "python"
    review_level: Literal["low", "medium", "high"] = "medium"

class ReviewResponse(BaseModel):
    request_id: str
    comments: list[ReviewComment]
    summary: str
    total_issues: int
```

### 3. 练习 (1h)

- [x] 写测试用例验证：
  - 合法数据正常解析
  - 非法数据（负数行号、空 message）抛出 ValidationError
  - model_validate() 和 model_dump() 往返
- [x] 把 Schema 写入 `projects/code-review-agent/src/schemas.py`

### 4. 输出 (0.5h)
- 笔记整理到 `notes/engineering/python-types.md`

## LeetCode 每日

- [x] 两数之和 (Easy)
- [x] 有效的括号 (Easy)

---

## 完成情况

<!-- 当天结束时填写 -->

- 实际用时: 4.5h
- 完成度: 100%
- 卡住的地方: 如何取出列表最后一个元素：lst[-1]

## 每日自检（23:00 前完成）

白纸默写今天学到的 3 个关键点：
1. 类型注解只是一个提示，本身不具备强制力
2. schema 中数据类基本都是继承 BaseModel 以获取类型检查能力和方便的序列化反序列化
3. 学会使用字典来取代复杂的 if elif elif elif else

写出来几条？__3__ / 3

- [x] 3/3: 正常推进
- [ ] 2/3: 明早先花 30 分钟复习
- [ ] 1/3: 明天重做今天的内容
- [ ] 0/3: 明天重做 + 事后分析为什么无效

## 明天计划

day04 asyncio 异步编程

---
*打卡时间: 23:59 前 commit*
